To run the script named basic_setup.sh, you need to execute it from the terminal. Here are the steps:

Open a terminal window.

Navigate to the directory where the basic_setup.sh script is located using the cd command. For example, if the script is located in the /home/user/scripts directory, you would use:

cd /home/user/scripts

Once you're in the directory containing the script, you can execute it by typing:

./basic_setup.sh
Press Enter, and the script will start executing. It will display the basic system information as defined in the script.

Make sure the script has executable permissions. If it doesn't, you can grant it permission using the chmod command:

chmod +x basic_setup.sh